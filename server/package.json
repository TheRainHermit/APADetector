{
  "name": "apadetector-server",
  "version": "1.0.0",
  "description": "Backend para APADetector (análisis de normas APA)",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --experimental-modules server.js",
    "dev": "nodemon --experimental-modules server.js",
    "production": "pm2 start ecosystem.config.js --env production",
    "development": "pm2 start ecosystem.config.js --env development",
    "stop:prod": "pm2 stop apadetector-server",
    "restart:prod": "pm2 restart apadetector-server",
    "delete:prod": "pm2 delete apadetector-server",
    "monitor": "pm2 monit",
    "logs": "pm2 logs apadetector-server"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mammoth": "^1.9.1",
    "multer": "^1.4.4",
    "mysql2": "^3.10.0",
    "pdf-parse": "^1.1.1",
    "textract": "^2.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
